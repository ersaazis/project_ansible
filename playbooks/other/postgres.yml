- name: Backup PostgreSQL
  hosts: all
  become: yes
  roles:
    - role: postgres
      tags: [backup]
      when: 
        - "database_server | default('') == 'postgres'"
        - "enable_backup | default(false) | bool"
