- name: Setup MySQL
  hosts: all
  become: yes
  roles:
    - role: mysql_install
      when: database_server | default('') == 'mysql'
    - role: mysql_config
      when: database_server | default('') == 'mysql'

