- name: Backup MySQL
  hosts: all
  become: yes
  roles:
    - role: mysql
      tags: [backup]
      when: 
        - "database_server | default('') == 'mysql'"
        - "enable_backup | default(false) | bool"
